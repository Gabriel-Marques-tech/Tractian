[pytest]
testpaths = tests
# rootdir e a raiz do repo para que `agent` seja importavel sem instalacao.
# Os testes da API industrial ficam em api/tests e rodam a partir de api/.
addopts = -q
markers =
    slow: exercita modelo de verdade; nao roda por padrao
